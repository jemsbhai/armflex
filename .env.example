# armflex requires no environment variables at this time.
# This file exists to satisfy the project scaffolding standard. Add
# placeholder entries here if configuration is ever introduced; real
# values go in .env, which is gitignored.
